"""Basic setup."""
from setuptools import setup


setup(name="densecnn",
      version="0.0.1",
      description="Dense CNN local image features.",
      author="Dave Greenwood",
      zip_safe=False,
      py_modules=["densecnn"],
      )
